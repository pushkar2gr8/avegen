import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  loginContainer: {
    height: 150,
    justifyContent: 'space-around',
    marginTop: 10,
  },
});
